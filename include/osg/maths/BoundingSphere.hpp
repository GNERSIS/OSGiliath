/* OSGiliath — OpenSceneGraph fork. See LICENSE.txt.
 * Convenience typedef for the bounding sphere.
 * Wraps maths::sphere<float> for view frustum culling and LOD.
 */
#pragma once

// Legacy header — redirects to new math types
#include <osg/maths/sphere.hpp>
