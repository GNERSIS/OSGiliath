/* OSGiliath — OpenSceneGraph fork. See LICENSE.txt.
 * Convenience typedef for the axis-aligned bounding box.
 * Wraps maths::box<float> for scene graph bounds computation.
 */
#pragma once

// Legacy header — redirects to new math types
#include <osg/maths/box.hpp>
