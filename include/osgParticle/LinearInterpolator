/* OSGiliath — OpenSceneGraph fork. See LICENSE.txt.
 * Linear interpolation between two values over particle lifetime.
 * Used for color, size, and alpha transitions.
 */
//osgParticle - Copyright (C) 2002 Marco Jez

#pragma once

#include <osgParticle/Interpolator>

#include <osg/core/Inherit.hpp>
#include <osg/core/CopyOp.hpp>
#include <osg/core/Object.hpp>
#include <osg/maths/vec3.hpp>
#include <osg/maths/vec4.hpp>

namespace osgParticle
{

    /// A linear interpolator.
    class LinearInterpolator: public osg::Inherit<Interpolator, LinearInterpolator>
    {
    public:
        LinearInterpolator() {}

        LinearInterpolator(const LinearInterpolator& copy, const osg::CopyOp& copyop = osg::CopyOp::SHALLOW_COPY)
        : Inherit(copy, copyop) {}

        OSG_REGISTER_TYPE(osgParticle, LinearInterpolator)

        virtual float interpolate(float t, float y1, float y2) const
        {
            return y1 + (y2 - y1) * t;
        }

        virtual osg::vec2 interpolate(float t, const osg::vec2& y1, const osg::vec2& y2) const
        {
            return y1 + (y2 - y1) * t;
        }

        virtual osg::vec3 interpolate(float t, const osg::vec3& y1, const osg::vec3& y2) const
        {
            return y1 + (y2 - y1) * t;
        }

        virtual osg::vec4 interpolate(float t, const osg::vec4& y1, const osg::vec4& y2) const
        {
            return y1 + (y2 - y1) * t;
        }

    protected:
        virtual ~LinearInterpolator() {}
    };

}
