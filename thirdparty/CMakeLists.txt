# Restored stub — original thirdparty/ was gitignored and lost.
# cmake/Tidy.cmake's header-filter comment suggests FetchContent deps lived
# under thirdparty/_deps; nothing in src/, include/, examples/, or
# applications/ references a thirdparty target, so an empty stub suffices
# for configure + build.
